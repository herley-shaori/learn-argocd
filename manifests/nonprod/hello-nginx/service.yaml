apiVersion: v1
kind: Service
metadata:
  name: hello-nginx
  namespace: hello-nginx-nonprod
  labels:
    app: hello-nginx
    env: nonprod
spec:
  type: ClusterIP
  selector:
    app: hello-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
